apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: redis-backup
  namespace: week-infra
spec:
  accessModes: ["ReadWriteOnce"]
  storageClassName: local-path
  resources:
    requests:
      storage: 1Gi
