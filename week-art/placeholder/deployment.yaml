apiVersion: apps/v1
kind: Deployment
metadata:
  name: week-art-placeholder
spec:
  replicas: 1
  selector:
    matchLabels:
      app: week-art-placeholder
  template:
    metadata:
      labels:
        app: week-art-placeholder
    spec:
      containers:
        - name: placeholder
          image: nginx:1.27-alpine
          ports:
            - containerPort: 80
          volumeMounts:
            - name: html
              mountPath: /usr/share/nginx/html
              readOnly: true
          livenessProbe:
            httpGet:
              path: /
              port: 80
            initialDelaySeconds: 5
            periodSeconds: 10
            timeoutSeconds: 2
          readinessProbe:
            httpGet:
              path: /
              port: 80
            initialDelaySeconds: 3
            periodSeconds: 10
            timeoutSeconds: 2
          imagePullPolicy: IfNotPresent
      volumes:
        - name: html
          configMap:
            name: week-art-placeholder-html
